---
import logoSrc from "@/assets/images/logo.svg?url";

interface Props {
  variant?: "default" | "reversed";
  class?: string;
}
const { class: cls = "" } = Astro.props;
---

<img src={logoSrc} alt="BayanatiHub" class={cls} />
